# 개념 및 수식 참조 가이드

**엔진 번호**: UP-1  
**버전**: 1.2.0  
**목적**: 전체 레이어의 개념과 수식을 체계적으로 정리

---

## 📚 목차

1. [핵심 철학](#핵심-철학)
2. [레이어별 개념](#레이어별-개념)
3. [수식 정리](#수식-정리)
4. [다음 레이어 (L3) 개념 설계](#다음-레이어-l3-개념-설계)

---

## 🎯 핵심 철학

### 설계 원칙

> **"진단서(원인)와 처방전(해결)은 분리되어야 함"**

- L0-L2: 원인 분석 및 실패 지형 형성 (진단)
- L3: 해결 탐색 (처방)

### 뇌의 20mV 원리

> **"뇌는 정답을 강화하지 않는다. 뇌는 틀린 길을 조용히 없앤다."**

- 대부분의 뉴런은 침묵(silent) 상태
- 실패는 발화로 이어지지 않음 (LTD)
- 정보는 "전위 지형"으로 저장됨

### 엔진의 정체성

> **ThreeBodyBoundaryEngine은 LLM을 "항상 발화하는 뇌"에서 "대부분 침묵하는 뇌"로 바꾸는 전위 지형 생성기다.**

---

## 🏛️ 레이어별 개념

### L0: 원인 분석 레이어 (법칙)

**역할**: "왜 실패하는지" 분석

**핵심 개념**:
- **경계 정합 관점**: 안정 궤도 = 경계-공간 정합 상태
- **혼돈 = 경계 refinement 실패**
- **π 개념**: 정합 계수 붕괴

**입력**: `ThreeBodySystem` (3개 체의 위치, 질량)

**출력**: `StabilityAnalysis`
- `mismatch`: 경계 불일치 (Δ)
- `stability_score`: 안정성 점수
- `convergence_rate`: 수렴 속도
- `is_stable()`: 안정 여부

**수식**:
```
V(x,y) = -G × Σ(m_i / r_i)          # 중력 퍼텐셜
ρ(x,y) = V(x,y) / V_max              # 밀도 변환 (정규화)
Δ = |P - 2πr| / 2πr + |A - πr²| / πr²  # 경계 정합 계산
```

**안정성 판정**:
```
안정: Δ < threshold AND convergence_rate > 0
불안정: Δ > threshold OR convergence_rate < 0
```

---

### L1: 실패 추적 레이어 (기억)

**역할**: "어디서 실패하는지" 기록

**핵심 개념**:
- **Failure Atlas**: 실패 지도
- **Condition Signature**: 조건 서명 (구조적 특징)
- **Collapse Mode**: 붕괴 모드 분류

**입력**: 
- `ThreeBodySystem` (시스템 상태)
- `StabilityAnalysis` (분석 결과)
- `spatial_pattern` (공간 패턴)

**출력**: `FailureRecord`
- `condition_signature`: 조건 서명
- `collapse_mode`: 붕괴 모드
- `collapse_severity`: 붕괴 심각도 (0.0 ~ 1.0)
- `spatial_pattern`: 공간 패턴

**수식**:
```
# Condition Signature 생성
signature = hash(
    normalize(mass_ratios) + 
    normalize(distance_patterns) +
    normalize(angular_momenta)
)

# 유사도 계산
similarity = cosine_similarity(sig1, sig2)

# 심각도 계산
severity = (mismatch / max_mismatch) × (1 - stability_score)
```

**붕괴 모드 분류**:
```
CONVERGENCE_FAILURE: convergence_rate < 0
MISMATCH_THRESHOLD_EXCEEDED: Δ > threshold
STABILITY_SCORE_CRITICAL: stability_score < 0.3
```

---

### L2: 실패 학습 레이어 (본능)

**역할**: "어디를 피해야 하는지" 형성

**핵심 개념**:
- **Search Bias**: 탐색 편향
- **Risk Map**: 위험 지도
- **STDP-like mechanism**: 실패 빈도에 따른 위험도 조절

**입력**: `FailureAtlas` (L1의 실패 기록)

**출력**: `SearchBias`
- `risk_map`: 조건 서명별 위험도
- `collapse_mode_risks`: 붕괴 모드별 위험도
- `total_risk_score`: 총 위험도

**수식**:
```
# 위험도 계산
risk(condition) = frequency_weight × freq(condition) + 
                  severity_weight × avg_severity(condition)

# STDP-like 감쇠
risk(t) = risk(t-1) × decay_factor^age

# 회피 판정
should_avoid = risk(condition) > threshold
```

**⚠️ 중요**: 
> **본 레이어는 어떠한 정책, 행동, 해결책도 생성하지 않습니다.  
> 오직 탐색 공간에서의 위험 지형만을 형성합니다.**

---

## 📐 수식 정리

### 1. 중력 퍼텐셜 → 밀도 변환

```
V(x,y) = -G × Σ(m_i / r_i)

여기서:
- G: 중력 상수
- m_i: i번째 체의 질량
- r_i: (x,y)에서 i번째 체까지의 거리

정규화:
V_max = max(V(x,y)) for all (x,y)
ρ(x,y) = V(x,y) / V_max  (0 ≤ ρ ≤ 1)
```

### 2. 경계 정합 계산

```
P = 경계 둘레 (perimeter)
A = 경계 내부 면적 (area)
r = 기대 반지름

Δ(경계, 공간) = |P - 2πr| / 2πr + |A - πr²| / πr²

여기서:
- 첫 번째 항: 둘레 불일치
- 두 번째 항: 면적 불일치
- Δ = 0: 완벽한 정합
- Δ > threshold: 불안정
```

### 3. 안정성 점수

```
stability_score = 1 - min(Δ, 1.0) × (1 - convergence_rate)

여기서:
- convergence_rate > 0: 수렴 중
- convergence_rate < 0: 발산 중
- stability_score ∈ [0, 1]
```

### 4. Condition Signature

```
# 질량 비율 정규화
mass_ratios = [m1/m_total, m2/m_total, m3/m_total]

# 거리 패턴 정규화
distances = [r12, r23, r31]
distance_pattern = normalize(distances)

# 각운동량 정규화
angular_momenta = [L1, L2, L3]
angular_pattern = normalize(angular_momenta)

# 서명 생성
signature = hash(
    concat(mass_ratios, distance_pattern, angular_pattern)
)
```

### 5. 위험도 계산 (L2)

```
# 빈도 기반 위험도
freq_risk = count(condition) / total_failures

# 심각도 기반 위험도
severity_risk = avg(severity(condition))

# 통합 위험도
risk(condition) = α × freq_risk + β × severity_risk

여기서:
- α = frequency_weight (기본값: 0.6)
- β = severity_weight (기본값: 0.4)
- α + β = 1.0
```

### 6. STDP-like 감쇠

```
risk(t) = risk(t-1) × decay_factor^age

여기서:
- decay_factor = 0.9 (기본값)
- age = 현재 시간 - 실패 발생 시간
- 오래된 실패일수록 영향 감소
```

---

## 🚀 다음 레이어 (L3) 개념 설계

### L3: 해결 탐색 레이어 (지능)

**역할**: "안전한 경로를 찾는" 탐색

**핵심 철학**:
> **L0-L2가 만든 "위험 지형" 위에서 안전한 경로를 탐색한다.**

**입력**:
- `SearchBias` (L2의 위험 지형)
- `ThreeBodySystem` (초기 조건)
- `target_state` (목표 상태, 선택적)

**출력**: `SolutionPath`
- `safe_conditions`: 안전한 조건 시퀀스
- `path_risk`: 경로 위험도
- `convergence_guarantee`: 수렴 보장 여부

**핵심 개념**:

#### 1. 안전 영역 탐색

```
safe_region = {condition | risk(condition) < safe_threshold}

여기서:
- safe_threshold = 0.3 (기본값)
- L2의 risk_map을 기반으로 필터링
```

#### 2. 경로 위험도

```
path_risk = Σ risk(condition_i) / path_length

여기서:
- path_length: 경로 길이
- 낮을수록 안전한 경로
```

#### 3. 수렴 보장

```
convergence_guarantee = all(
    stability_score(condition_i) > min_stability
    for condition_i in path
)

여기서:
- min_stability = 0.7 (기본값)
- 모든 조건이 안정적이어야 수렴 보장
```

#### 4. 탐색 전략

**전략 1: 그리디 탐색**
```
next_condition = argmin(risk(condition)) 
                 for condition in neighbors(current)
```

**전략 2: A* 유사 탐색**
```
f(condition) = g(condition) + h(condition)

여기서:
- g(condition): 시작점부터의 실제 위험도
- h(condition): 목표점까지의 추정 위험도
```

**전략 3: 확률적 탐색**
```
P(select condition) = exp(-risk(condition) / temperature)

여기서:
- temperature: 탐색 온도 (높을수록 탐험적)
- 위험도가 낮을수록 선택 확률 높음
```

#### 5. 동적 경로 조정

```
if risk(current_path) > max_path_risk:
    # 경로 재계산
    alternative_path = find_safer_path(
        start=current_position,
        goal=target_state,
        bias=search_bias
    )
```

### L3 수식 요약

```
# 안전 영역 정의
safe_region = {c | risk(c) < τ_safe}

# 경로 위험도
R_path = (1/n) × Σ risk(c_i)

# 수렴 보장
C_guarantee = ∀c ∈ path: stability(c) > θ_stable

# 탐색 함수 (A* 유사)
f(c) = g(c) + h(c)
     = Σ risk(c_i) + heuristic(c, goal)
```

### L3의 제약 조건

**⚠️ 아키텍처 원칙**:
1. **L2의 위험 지형을 절대 위반하지 않음**
   - `risk(condition) > threshold`인 조건은 탐색하지 않음
   
2. **L0의 물리 법칙을 절대 위반하지 않음**
   - `is_impossible(system)`인 조건은 탐색하지 않음

3. **해결책을 "강제"하지 않음**
   - 안전한 경로가 없으면 "없음"을 반환
   - 무리한 최적화 시도 안 함

---

## 🔗 레이어 간 연결

### 데이터 흐름

```
입력 (ThreeBodySystem)
  ↓
L0: 원인 분석
  ↓ StabilityAnalysis
L1: 실패 기록
  ↓ FailureAtlas
L2: 위험 지형 형성
  ↓ SearchBias
L3: 안전 경로 탐색
  ↓ SolutionPath
출력 (안전한 해결 경로)
```

### 의존성

```
L0: 독립 (물리 법칙)
L1: L0 의존 (분석 결과 필요)
L2: L1 의존 (실패 기록 필요)
L3: L0, L2 의존 (법칙 + 위험 지형 필요)
```

---

## 📊 개념 매핑

### 뇌의 구조와 대응

| 뇌의 구조 | 엔진 레이어 | 역할 |
|----------|-----------|------|
| 물리 법칙 | L0 | 불가능한 영역 차단 |
| 기억 (해마) | L1 | 실패 패턴 기억 |
| 본능 (소뇌) | L2 | 위험 회피 지형 |
| 지능 (전두엽) | L3 | 안전 경로 탐색 |

### STDP 메커니즘과 대응

| STDP | 엔진 | 메커니즘 |
|------|------|---------|
| LTD (장기 억제) | L2 | 실패 경로 위험도 증가 |
| LTP (장기 강화) | L3 | 안전 경로 선택 |
| 시냅스 약화 | L2 | 위험 지형 형성 |
| 시냅스 강화 | L3 | 안전 경로 강화 |

---

## 🎯 핵심 정리

### 레이어별 역할

- **L0 (법칙)**: "이것은 불가능하다"
- **L1 (기억)**: "여기서 실패했다"
- **L2 (본능)**: "여기는 위험하다"
- **L3 (지능)**: "여기는 안전하다"

### 수식 체계

1. **물리 법칙** (L0): 중력 퍼텐셜, 경계 정합
2. **기억 구조** (L1): Condition Signature, 유사도
3. **위험 지형** (L2): Risk Map, STDP-like 감쇠
4. **탐색 전략** (L3): 안전 영역, 경로 위험도

### 아키텍처 원칙

> **"진단서(원인)와 처방전(해결)은 분리되어야 함"**

- L0-L2: 진단 (왜, 어디서, 어떻게 피할지)
- L3: 처방 (어디로 갈지)

---

**작성자**: GNJz (Qquarts)  
**작성일**: 2026-02-03  
**버전**: 1.2.0

