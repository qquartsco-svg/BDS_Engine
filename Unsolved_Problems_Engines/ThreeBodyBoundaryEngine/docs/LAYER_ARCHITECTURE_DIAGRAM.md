# 레이어 구조 다이어그램

**엔진**: ThreeBodyBoundaryEngine (UP-1)  
**버전**: 1.2.0  
**최종 업데이트**: 2026-02-03

---

## 🏛️ 전체 레이어 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    ThreeBodyBoundaryEngine                    │
│                      (레이어 아키텍처)                         │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  L0: 원인 분석 레이어 (Existence/Possibility Layer)          │
│  ────────────────────────────────────────────────────────    │
│  역할: "왜 실패하는지" (법칙)                                 │
│  모듈: ThreeBodyBoundaryEngine                                │
│  출력: StabilityAnalysis                                     │
│  ────────────────────────────────────────────────────────    │
│  • 혼돈 원인 추적                                            │
│  • 경계 정합 실패 분석                                        │
│  • 붕괴 조건 식별                                             │
│  ⚠️ 해결 시도 안 함                                           │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ StabilityAnalysis
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  L1: 실패 추적 레이어 (Failure Accumulation Layer)          │
│  ────────────────────────────────────────────────────────    │
│  역할: "어디서 실패하는지" (기억)                             │
│  모듈: FailureAtlas                                          │
│  출력: FailureRecord, Failure Statistics                     │
│  ────────────────────────────────────────────────────────    │
│  • 실패 패턴 구조화                                           │
│  • 조건 서명 생성                                             │
│  • 유사한 실패 패턴 찾기                                      │
│  • 붕괴 모드 분류                                             │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ FailureAtlas
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  L2: 실패 학습 레이어 (Failure Learning Layer)               │
│  ────────────────────────────────────────────────────────    │
│  역할: "어디를 피해야 하는지" (본능)                          │
│  모듈: FailureBiasConverter                                  │
│  출력: SearchBias                                            │
│  ────────────────────────────────────────────────────────    │
│  • 실패 → 편향 변환                                           │
│  • 위험 지도 생성                                             │
│  • 조건 회피 판정                                             │
│  • STDP 유사 메커니즘                                         │
│  ⚠️ 정책/행동/해결책 생성 안 함                               │
│  ⚠️ 오직 위험 지형만 형성                                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ SearchBias
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  L3: 해결 탐색 레이어 (Solution Exploration Layer)           │
│  ────────────────────────────────────────────────────────    │
│  역할: "안전한 평지를 찾는다" (지능)                          │
│  상태: ⛔ 의도적으로 분리 (아직 구현 안 함)                  │
│  ────────────────────────────────────────────────────────    │
│  • 안전한 조건 탐색                                           │
│  • 최적 경로 계산                                             │
│  • 해결책 제시                                               │
│  ⚠️ L1, L2 완성 후에만 구현                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📊 데이터 흐름도

### 전체 파이프라인

```
[입력]
  │
  │ ThreeBodySystem
  ▼
┌─────────────────────┐
│  L0: 원인 분석       │
│  ────────────────    │
│  • 중력 퍼텐셜 계산  │
│  • 밀도 변환         │
│  • 경계 형성 시뮬    │
│  • 안정성 분석        │
└─────────────────────┘
  │
  │ StabilityAnalysis
  │  - converged: bool
  │  - mismatch: float
  │  - stability_score: float
  │  - convergence_rate: float
  ▼
┌─────────────────────┐
│  L1: 실패 추적       │
│  ────────────────    │
│  • 실패 기록 생성     │
│  • 조건 서명 생성     │
│  • 붕괴 모드 분류     │
│  • 유사 패턴 찾기     │
└─────────────────────┘
  │
  │ FailureAtlas
  │  - failure_records: List[FailureRecord]
  │  - failure_manifold: Dict[str, List[FailureRecord]]
  │  - collapse_taxonomy: Dict[str, int]
  ▼
┌─────────────────────┐
│  L2: 실패 학습       │
│  ────────────────    │
│  • 위험 지도 생성     │
│  • 편향 가중치 계산   │
│  • 회피 판정          │
│  • 안전 조건 필터링   │
└─────────────────────┘
  │
  │ SearchBias
  │  - risk_map: Dict[str, float]
  │  - collapse_mode_risk: Dict[str, float]
  │  - total_risk_score: float
  ▼
[출력]
  → L3로 전달 (미래)
```

---

## 🔄 레이어 간 상호작용

### L0 → L1 통합

```
ThreeBodyBoundaryEngine
    │
    │ analyze_orbit_stability()
    │ → StabilityAnalysis
    │
    ▼
FailureAtlas
    │
    │ record_failure(
    │     analysis: StabilityAnalysis,
    │     system: ThreeBodySystem
    │ )
    │ → Optional[FailureRecord]
```

**예제**:
```python
# L0 분석
analysis = engine.analyze_orbit_stability(system)

# L1 기록
record = atlas.record_failure(analysis, system, threshold=0.1)
```

---

### L1 → L2 통합

```
FailureAtlas
    │
    │ failure_records
    │ failure_manifold
    │ collapse_taxonomy
    │
    ▼
FailureBiasConverter
    │
    │ convert_failure_to_bias(
    │     failure_atlas: FailureAtlas
    │ )
    │ → SearchBias
```

**예제**:
```python
# L1 → L2 변환
bias = converter.convert_failure_to_bias(atlas)
```

---

### L2 → L3 (미래)

```
SearchBias
    │
    │ risk_map
    │ collapse_mode_risk
    │
    ▼
SolutionExplorer (미래)
    │
    │ explore_safe_solutions(
    │     bias: SearchBias,
    │     candidates: List[Condition]
    │ )
    │ → List[Solution]
```

---

## 🎯 레이어별 역할 요약

### L0: 원인 분석 (법칙)

**입력**: `ThreeBodySystem`  
**출력**: `StabilityAnalysis`  
**역할**: "왜 실패하는지"

**특징**:
- 순수한 관찰만 수행
- 학습 기능 없음
- 해결책 제시 없음
- 자연법칙에 해당

---

### L1: 실패 추적 (기억)

**입력**: `StabilityAnalysis`, `ThreeBodySystem`  
**출력**: `FailureRecord`, `Failure Statistics`  
**역할**: "어디서 실패하는지"

**특징**:
- 실패를 구조로 누적
- 조건 서명으로 패턴 식별
- 유사한 실패 패턴 찾기
- "혼돈은 랜덤이 아니다" 증명

---

### L2: 실패 학습 (본능)

**입력**: `FailureAtlas`  
**출력**: `SearchBias`  
**역할**: "어디를 피해야 하는지"

**특징**:
- 실패 → 편향 변환
- 위험 지도 생성
- 조건 회피 판정
- STDP 유사 메커니즘

---

### L3: 해결 탐색 (지능) - 미래

**입력**: `SearchBias`, `Candidate Conditions`  
**출력**: `List[Solution]`  
**역할**: "안전한 평지를 찾는다"

**특징**:
- 안전한 조건 탐색
- 최적 경로 계산
- 해결책 제시
- ⚠️ L1, L2 완성 후에만 구현

---

## 🔐 레이어 독립성

### 각 레이어는 독립적으로 동작 가능

```
L0: ThreeBodyBoundaryEngine
    └─ 독립 실행 가능 ✅

L1: FailureAtlas
    └─ L0 없이도 테스트 가능 ✅
    └─ (단, L0의 출력을 입력으로 받음)

L2: FailureBiasConverter
    └─ L1 없이도 테스트 가능 ✅
    └─ (단, L1의 출력을 입력으로 받음)
```

### 레이어 간 의존성

```
L0 ← 독립적 (자연법칙)
L1 ← L0 의존 (L0의 출력 필요)
L2 ← L1 의존 (L1의 출력 필요)
L3 ← L1, L2 의존 (미래)
```

---

## 📈 레이어 확장성

### 새로운 레이어 추가 시

1. **L0는 절대 건드리지 않음** ✅
2. **새 레이어는 기존 레이어 위에 얹음** ✅
3. **인터페이스 명확히 정의** ✅

### 예시: L3 추가 시

```
기존:
L0 → L1 → L2

추가 후:
L0 → L1 → L2 → L3
         ↑
    (L0, L1, L2는 변경 없음)
```

---

## 🧠 핵심 원칙

### "L0는 법칙, L1은 기억, L2는 본능, L3는 지능이다"

- **L0 (법칙)**: 자연법칙에 해당, 절대 변경 금지
- **L1 (기억)**: 실패를 구조로 기억
- **L2 (본능)**: 실패를 편향으로 변환
- **L3 (지능)**: 안전한 해결책 탐색

---

**작성자**: GNJz (Qquarts)  
**최종 업데이트**: 2026-02-03

